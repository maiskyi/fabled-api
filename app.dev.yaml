runtime: nodejs20
service: api-dev
instance_class: F1

entrypoint: node dist/main

beta_settings:
  cloud_sql_instances: fabled-976c8:us-east1:development

automatic_scaling:
  min_instances: 1
  max_instances: 1

env_variables:
  SERVER_CORS_ORIGINS: "http://localhost:5173,https://app.dev.fabled.space,capacitor://localhost"
